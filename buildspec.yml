version: 0.2

phases:
  pre_build:
    commands:
      - aws --version
      - npm install
  build:
    commands:
      - npm run build
      - echo $S3_BUCKET_NAME
      - aws s3 sync build/ s3://$S3_BUCKET_NAME