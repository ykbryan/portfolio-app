version: 0.2

phases:
  pre_build:
    commands:
      - npm install
  build:
    commands:
      - npm run build
  post_build:
    commands:
      - rm -r !(build)
    finally:
      - cd build
      - mv * ../

artifacts:
    files: '**/*'